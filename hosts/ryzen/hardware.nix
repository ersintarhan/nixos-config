# PLACEHOLDER - Replace this file on the ryzen machine with:
# nixos-generate-config --show-hardware-config > ~/nixos-config/hosts/ryzen/hardware.nix
{ config, lib, pkgs, modulesPath, ... }:

{
  imports = [ ];

  # This is a placeholder. Generate the real hardware config on the target machine.
  # The command above will detect:
  # - CPU and kernel modules
  # - Filesystem mounts and UUIDs
  # - Boot settings
  # - Hardware-specific drivers
}
